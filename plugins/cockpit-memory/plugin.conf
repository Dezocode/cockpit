# Cockpit-native plugin manifest. This is intentionally not a Codex plugin
# manifest and is never passed to `codex plugin add`.
id=cockpit.memory
name=Intercom memory diagram
type=cockpit
version=1
entrypoint=memory
description=Render memory/cockpit.mmd from the Intercom clone with index, intercom, and hooks subgraphs only
