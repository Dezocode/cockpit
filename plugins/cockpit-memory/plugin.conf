# Cockpit-native plugin manifest. This is intentionally not a Codex plugin
# manifest and is never passed to `codex plugin add`.
id=cockpit.memory
name=Cockpit memory map
type=cockpit
version=1
entrypoint=memory
description=Render the Intercom memory map from ~/intercom/memory/cockpit.mmd
