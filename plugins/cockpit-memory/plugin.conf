# Cockpit-native plugin manifest. Codex marketplace plugins are separate.
id=cockpit.memory
name=Intercom memory map
type=cockpit
version=1
entrypoint=memory
description=Render the intercom-managed Cockpit memory projection
