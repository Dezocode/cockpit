# cockpit.bench

Read-only ghui drill over Proctor bench receipts (`runs.sqlite`, `MODELS_INDEX.csv`,
`CROSSREF_LINKS.csv`). Cockpit never writes scores; Proctor is the sole writer.

See `aspects/cockpit-bench.md` for the compiled contract.
