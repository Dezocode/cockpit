# Cockpit-native plugin manifest. Codex marketplace plugins are separate.
id=cockpit.bench
name=Benchmark run drill
type=cockpit
version=1
entrypoint=bench
description=Read-only Proctor mirror with a ghui-style model, run, and backlink drill
