# Cockpit-native plugin manifest. Codex marketplace plugins are separate.
id=cockpit.bench
name=Proctor bench drill
type=cockpit
version=1
entrypoint=bench
description=Render the Proctor-managed bench receipt as a read-only ghui drill (models → runs → backlinks)
