# Cockpit-native plugin manifest. This is intentionally not a Codex plugin
# manifest and is never passed to `codex plugin add`.
id=cockpit.cpr
name=Canonical live updater
type=cockpit
version=1
entrypoint=cpr
description=Validate and update the live Cockpit overlay without respawning agent panes
