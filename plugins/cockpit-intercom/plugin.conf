# Cockpit-native plugin manifest. This is intentionally not a Codex plugin
# manifest and is never passed to `codex plugin add`.
id=cockpit.intercom
name=GitHub Intercom peer bus
type=cockpit
version=1
entrypoint=intercom
description=Clone and publish peer state to Dezocode/intercom using the local gh identity
