# Cockpit-native plugin manifest. Codex marketplace plugins are separate.
id=cockpit.computers
name=Computer roster and models
type=cockpit
version=1
entrypoint=computers
description=Render the intercom-managed Cockpit computer receipt (read-only roster and MODELS subview)
