# t847u COCKPIT 2 — FORGE GOSPEL (authority)

Card: `cursor-cockpit2-tauri-t847u` · composer-2.5 · frontier_subscription  
DENY: local Qwen / sol-v1.7.1 · Funnel · secrets bake · force-push · product-socket TUI kill

## Baked pins

| Package | Pin |
|---------|-----|
| Tauri | 2.11.x (`@tauri-apps/api` 2.11.1, CLI 2.11.4) |
| React | 19 |
| Vite | 6 |
| dockview-react | 8.2.0 |
| react-resizable-panels | 4.12.3 |
| @xterm/xterm | 6 |
| tauri-plugin-pty | 0.3.1 |

## Architecture

- **6-chip bar only:** PRS · provider · FILES · MEMORY · MODEL · RESTART (no 7th)
- **MODELS** = view `m` inside COMPUTERS (not a 9th dockview window)
- **Foot** = sole size-owning on dezohost product socket; Funnel+Serve OFF
- **Ghostty/Foot** = shell-out registry (not fake-attach into xterm)
- **Splash** = GitHub OAuth device-flow; tokens in OS keyring / Stronghold only
- **Hostinger** = new Cockpit web deploy (install.sh + systemd + nginx + `/api/health`)
- **Hermes** = Deck receipt / COMPUTERS node — NOT an AGENT provider
- **KEEP** all TUI pages; isolated `TMUX_TMPDIR` tests; Origin ≠ intercom bus

## Done-line (morning CT)

1. Hostinger fresh install health green
2. Capability matrix green
3. t384u screenshots: splash, ≥20 agents, live term, COMPUTERS, MEMORY, Hostinger health

## Commands

```bash
./bench/cockpit/hostinger-h0-verify.sh
./bench/cockpit/capability-matrix.sh
./bench/cockpit/capture-screenshots.sh
COCKPIT_INSTALL_HOSTINGER=1 ./install.sh
```

Mirror: `bench/cursor/reports/t847u-cockpit2-gospel-prompt.md`
