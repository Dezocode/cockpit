# Cockpit live updater configuration. `cpr` reads this file; values are
# deliberately declarative and do not contain shell commands.

[cpr]
overlay=1
hooks=1
agent_validation=1
refresh_bar=signal
adapt_layout=0
refresh_derived=0
ensure_bar=0
require_agent_validation=0
