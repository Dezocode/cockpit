# t728u-next5 kick packet — BENCH bar complete (Proctor validation card)

**Card:** `cursor-bench-bar-complete-t728u-next5`  
**Branch:** `cursor/bench-ghui-next3-c666` (draft PR #5 — never merge/force-push)  
**Prior head:** `5a11d51`

## Prior FAIL CPRs

| Capture | Score ref | Issue |
|---|---|---|
| 010200 | live CPR t728u-next4 | Totoro/wallpaper bleed; single yellow region (corner-only edges ≠ separate boxes) |
| 010043 | local CPR t728u-next4 | harness PASS but dezohost pixel CPR still FAIL |
| 004830 | live CPR t728u-next3 | Totoro/wallpaper bleed; packed chip read; tmux 1:AGENT strip density clash |
| 004602 | local CPR t728u-next3 | harness PASS but pixel CPR on dezohost still FAIL |

**Approved target:** `/workspace/proofs/approved-style/bench-ghui-approved-t533u.png`

## This run (t728u-next5)

| Schema | Fix |
|---|---|
| multi-chip | `highlight_chip_box` full yellow border bands (top/bottom full-width + left/right body edges); `CHIP_GAP_ROWS=2` with `CockpitBenchFlat` gap rows |
| visual-density | `tmux set-option -w status off` on BENCH window — hides 1:AGENT 2:FILES strip |
| omarchy-theme | `paint_terminal_flat_bg` OSC 10/11 + global/window/pane tmux omarchy-flat; `pad_buffer_omarchy_flat` tail fill; VimEnter/FocusGained/ColorScheme/VimResized re-apply |
| touch+keyboard | SGR mouse on pane tty + dated `proofs/bench-touch-receipt-YYYYMMDD-HHMMSS.txt` with timestamp/coords; API `touch_probe` negative control; keyboard in `test-bench.sh` |
| resize | KEEP from 004830 — distinct sha256 at 220×40 and 160×32 |

## Harness tests

```bash
bash tests/test-bench-ghui-style.sh
bash tests/test-bench-agency-style.sh
bash tests/test-bench-complete-t728u.sh
bash tests/test-bench-touch-t728u.sh
bash tests/test-bench-cpr-t728u-next5.sh
bash tests/test-bench.sh   # requires nvim in PATH
```

## Live CPR (this run)

- Text CPR: generated by `test-bench-cpr-t728u-next5.sh`
- Multi-chip: 2× `worker_deck_run` in separate full-border 4-row chips with ≥3 blank spacer rows between
- Density: CPR capture must NOT contain `1:AGENT` / `2:FILES` tmux window strip
- Omarchy-flat: OSC terminal bg + tmux window/pane-style opaque; `pad_buffer_omarchy_flat`; no Totoro/wallpaper in pixel CPR
- Touch: `proofs/bench-touch-receipt-*.txt` with timestamp + click_line/click_col
- Footer: single `t524u ghui` nvim statusline
- Resize: wide ≠ narrow (distinct THIS stamp sha256 — KEEP)

## Live CPR path (Canon — dezohost)

1. Land draft PR #5 on dezohost (Dezocode merges; agent stays draft).
2. `CPR live BENCH` on dezohost cockpit session.
3. Capture screenshot to `/workspace/proofs/bench-bar-t728u-live-<timestamp>.png`.
4. Proof hash **must differ** from: approved PNG (t533u), CPR 010200, 010043, 004830, 004602, 003648, 002855, 001318.
5. Score live capture under `bench/schemas/ui/scores/t728u-live-<id>.yaml`.
6. Soft-steer only from Canon thereafter — no agent self-merge.

## Deny kick

`sol-v1.7.1`, `local/*`, `Qwen`
