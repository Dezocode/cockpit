# t728u-next4 kick packet — BENCH bar complete (Proctor validation card)

**Card:** `cursor-bench-bar-complete-t728u-next4`  
**Branch:** `cursor/bench-ghui-next3-c666` (draft PR #5 — never merge/force-push)  
**Prior head:** `2e2e49a`

## Prior FAIL CPRs

| Capture | Score ref | Issue |
|---|---|---|
| 004830 | live CPR t728u-next3 | Totoro/wallpaper bleed; packed chip read; tmux 1:AGENT strip density clash |
| 004602 | local CPR t728u-next3 | harness PASS but pixel CPR on dezohost still FAIL |
| 003648 | live CPR t728u-next2 | Totoro/wallpaper bleed; packed chip read |
| 002855 | live CPR t728u-next | Totoro/wallpaper bleed; packed chip read; density noisy |

**Approved target:** `/workspace/proofs/approved-style/bench-ghui-approved-t533u.png`

## This run (t728u-next4)

| Schema | Fix |
|---|---|
| multi-chip | 4-row chip boxes with `CockpitBenchChipEdge` corners + `CHIP_GAP_ROWS=2`; fill clipped to box width |
| visual-density | `tmux set-option -w status off` on BENCH window — hides 1:AGENT 2:FILES strip (quieter than 004830) |
| omarchy-theme | `paint_terminal_flat_bg` OSC 10/11 + global/window tmux omarchy-flat; re-apply on VimEnter/FocusGained/ColorScheme |
| touch+keyboard | SGR mouse on pane tty in `test-bench-touch-t728u.sh`; `CockpitBenchTouchReceipts` counter; keyboard in `test-bench.sh` |
| resize | KEEP from 004830 — distinct sha256 at 220×40 and 160×32 (e233c9cf ≠ 753cf7ff) |

## Harness tests

```bash
bash tests/test-bench-ghui-style.sh
bash tests/test-bench-agency-style.sh
bash tests/test-bench-complete-t728u.sh
bash tests/test-bench-touch-t728u.sh
bash tests/test-bench-cpr-t728u-next4.sh
bash tests/test-bench.sh   # requires nvim in PATH
```

## Live CPR (this run)

- Text CPR: generated by `test-bench-cpr-t728u-next4.sh`
- Multi-chip: 2× `worker_deck_run` in separate 4-row edge-box chips with ≥3 blank spacer rows between
- Density: CPR capture must NOT contain `1:AGENT` / `2:FILES` tmux window strip
- Omarchy-flat: OSC terminal bg + tmux window-style opaque; no Totoro/wallpaper text bleed
- Footer: single `t524u ghui` nvim statusline
- Resize: wide `e233c9cf…` ≠ narrow `753cf7ff…` (distinct THIS stamp sha256 — KEEP)

## Live CPR path (Canon — dezohost)

1. Land draft PR #5 on dezohost (Dezocode merges; agent stays draft).
2. `CPR live BENCH` on dezohost cockpit session.
3. Capture screenshot to `/workspace/proofs/bench-bar-t728u-live-<timestamp>.png`.
4. Proof hash **must differ** from: approved PNG (t533u), CPR 004830, 004602, 003648, 002855, 001318.
5. Score live capture under `bench/schemas/ui/scores/t728u-live-<id>.yaml`.
6. Soft-steer only from Canon thereafter — no agent self-merge.

## Deny kick

`sol-v1.7.1`, `local/*`, `Qwen`
