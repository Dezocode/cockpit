# t293u-next kick packet — BENCH bar agency (bc-2d888210 tweak map)

**Card:** `cursor-bench-bar-agency-t293u-next`  
**Branch:** `cursor/bench-ghui-7ed3`  
**PR:** #4 (draft only — never merge/ship/force-push)

## Prior FAIL (live CPR 233909)

Score: `bench/schemas/ui/scores/t293u-live-233909.yaml`

KEEP nesting/L3/fill; FIX tweak map:

| # | Schema | Fix |
|---|---|---|
| 1 | multi-chip | N chips when N backlinks in sqlite |
| 2 | top-chrome | product-nav weight; tmux-strip dominance absent |
| 3 | models-badges | cyan `>` on Models + ABSENT styling |
| 4 | visual-density | quieter than 233909; watermark not fighting L3 |
| 5 | touch+keyboard | dual-input proof |
| 6 | resize-no-obfuscate | ≥2 size proofs |
| 7 | final-outlook-legacy-clean | rename + subtract legacy on FAIL items |

## Implementation surface

- `stage/nvim/lua/config/cockpit-bench.lua`
- `tests/test-bench-agency-style.sh`
- `tests/test-bench.sh` nested + multi-backlink fixture rows

## Deny kick

`sol-v1.7.1`, `local/*`, `Qwen`

## Evidence required post-land

Live CPR on dezohost ≠ approved PNG ≠ 232336 ≠ 233909.
