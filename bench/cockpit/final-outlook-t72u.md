# final-outlook — cursor-cockpit2-t847u-residuals-t72u

Card: `bench/schemas/cards/cursor-cockpit2-t847u-residuals-t72u.card.yaml`  
Thesis: t754u mint · Release KEEP v2.1.4 · Install root `/opt/cockpit`

## Closed (this cook)

| Residual | Resolution |
|----------|------------|
| HTML stand-ins as live SPA PASS | Replaced by Playwright shots against `vite preview` + `app/dist` |
| Bootstrap health claimed as full API | `H0-HEALTH-CONTRACT.md`; `source` field distinguishes paths |
| Hostinger dist-server node deps | `install-hostinger.sh` runs `pnpm install --prod` post-rsync |
| t384u evidence missing in tree | `capture-screenshots.sh` emits live PNG + JSON under `bench/cockpit/screenshots/t384u/` |

## must_absent (do not reintroduce)

- Language that static HTML files or curl-only JSON constitute **live SPA** proof
- PASS claims where `packaging/health-server.js` is the **full API** without residual flag
- `checks.tui: missing` on Surface Foot regress paths
- Saul paths (`/opt/sai`, `/root/.grok`, `saul-go`) as install root
- local Qwen / sol-v1.7.1 on Hostinger grok-build lane
- Funnel ON · Serve ON on subscription envelope

## KEEP (unchanged)

- Release v2.1.4 gospel curl paths
- Surface Foot TUI (`bin/cockpit*`) — zero regression
- agents fixture ≥ 20 (24)
- frontier_subscription envelope · Composer 2.5 cook class
- Draft PR — Dezocode merges

## Verification

```bash
./bench/cockpit/hostinger-h0-verify.sh
./bench/cockpit/capability-matrix.sh
./bench/cockpit/surface-matrix.sh
./bench/cockpit/capture-screenshots.sh
```

## Evidence index

- Health contract: `bench/cockpit/H0-HEALTH-CONTRACT.md`
- Screenshots: `bench/cockpit/screenshots/t384u/MANIFEST.json`
- Canon manifest: `tmp/t847u/README.md`
