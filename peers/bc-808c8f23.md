# Canon CPR patch offer — MEMORY named page (bc-808c8f23)

**Agent:** bc-808c8f23-ba01-4b7d-acdd-473f90568a33  
**Branch:** `cursor/memory-page-cpr-8a33` → PR #1 (`rfc/sai-harness-profile-platform`)  
**Model:** Composer 2.5 (never Fast, never Auto)

## Goal 1 MEMORY — landed (Canon CPR nondestructive)

### What ships

- **Named page:** 7th tmux window `MEMORY` in the same bar as AGENT/FILES/DIFF/MAP/SETUP/PRS (`cockpit-main` bootstrap).
- **Not popup:** No F10 / prefix+M / prefix+y memory routing in `stage/tmux/cockpit.conf`.
- **MAP unnested:** MEMORY is its own window with `@cockpit_role memory`; `cockpit-adapt` treats it as auxiliary (like MAP/PRS), never joined into the Omarchy 2×2 pad.
- **Plugin:** `cockpit.memory` — `id=cockpit.memory`, `type=cockpit`, `entrypoint=memory` (same manifest shape as `cockpit.intercom`).
- **Source contract:** `~/intercom/memory/cockpit.mmd` first, then cwd-relative `memory/cockpit.mmd` (`COCKPIT_PROJECT`). Fail closed if missing. Renders `index` / `intercom` / `hooks` subgraphs only. No `/workspace/aspects` scrape; no Foundry/PiSai/MBA in output.
- **Watcher:** `cockpit-memory-watch` — idle when off-screen, event-driven refresh, Show Me / Mermaid render path shared with MAP.
- **CPR:** `preserved=Agent,FILES,SETUP,DIFF,MAP,MEMORY`; ordinary apply does not respawn panes; `--refresh-derived` may respawn DIFF/MAP/MEMORY.
- **Routing:** `cockpit-touch`, `cockpit-wake`, `cockpit-reload-views`, `cockpit-help`, `cockpit-audit` topology check.
- **TUI pack:** `tests/test-tui-regression.sh` — original 6 + MEMORY (7 windows), fail-closed on nest (MEMORY not in MAP, pages not in AGENT).

### Tests (PREP + regression)

| Test | Status |
|------|--------|
| `tests/test-tui-regression.sh` | PASS (7-window, nest fail-closed) |
| `tests/test-memory-plugin.sh` | PASS (fail-closed, cwd fallback, CPR no-respawn) |
| `tests/test-memory-window.sh` | PASS (isolated MEMORY pane, Agent untouched) |
| `tests/test-termius-touch.sh` | PASS |
| `tests/test-cpr-plugin.sh` | PASS |

### Gap list for Thesis

| Item | Status | Notes |
|------|--------|-------|
| Named MEMORY page | **Landed** | 7th tmux window, TUI bar |
| MAP unnest | **Landed** | Auxiliary window; audit + regression enforce |
| mmd fail-closed | **Landed** | Intercom then cwd; explicit error in watcher |
| Plugin `cockpit.memory` | **Landed** | Registry, install.sh, audit health |
| TUI pack | **Landed** | 7-window contract + nest guards |
| Omarchy adapter host | **Landed** | `cockpit-adapt` hides MEMORY from mobile tab row; pad layout does not join MEMORY |
| Touch bar MEMORY button | **Gap** | No dedicated chip in `cockpit-bar` (4-chip row: PRS/AGENT/MODEL/RESTART); reach via tmux window list / prefix menu |
| Intercom clone on first launch | **Gap** | MEMORY shows error until `intercom sync`; no auto-fetch in watcher |
| Goal 2 | **Not started** | Per instruction |

### Copy path

If this file is not pushed to `Dezocode/intercom`, Canon can copy from the PR #1 goal-report commit body or `peers/bc-808c8f23.md` in the cockpit repo.
