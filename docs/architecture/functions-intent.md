> MODEL-WRITTEN — not regenerated by scripts/generate-arch-docs.sh. Edit freely.

# Function intents — cockpit

One-line, human-authored statement of what each function is for. Anchors match the `[intent]` links in `functions.md`.

| Function | Intent |
|---|---|
| <a id="readjson"></a>`readJson` — app/server/index.ts | Read a JSON fixture file from `fixtures/`, falling back to a default value when absent. |
| <a id="ghauthstatus"></a>`ghAuthStatus` — app/server/index.ts | Probe `gh auth status` to report whether the GitHub CLI is authenticated and as whom. |
| <a id="healthcheck"></a>`healthCheck` — app/server/index.ts | Compose the green/yellow health contract (TUI, fixtures, gh auth, web build) for `GET /api/health`. |
| <a id="health_check"></a>`health_check` — app/src-tauri/src/lib.rs | Tauri command returning the desktop app's static health payload. |
| <a id="gh_auth_status"></a>`gh_auth_status` — app/src-tauri/src/lib.rs | Tauri command that shells out to `gh auth status` and parses the logged-in account. |
| <a id="run"></a>`run` — app/src-tauri/src/lib.rs | Boot the Tauri application with opener, shell, and pty plugins plus the command handlers. |
| <a id="main"></a>`main` — app/src-tauri/src/main.rs | Binary entry point; delegates to the Tauri `run()` in lib.rs. |
| <a id="app"></a>`App` — app/src/App.tsx | Root React component wiring the router: splash screen vs. the cockpit workspace shell. |
| <a id="agentbar"></a>`AgentBar` — app/src/components/AgentBar.tsx | Top chip toolbar for switching pages/providers; highlights the active page. |
| <a id="tone"></a>`tone` — app/src/components/AgentBar.tsx | Pick cyan/yellow chip styling based on which page is currently active. |
| <a id="label"></a>`label` — app/src/components/AgentBar.tsx | Resolve the display label for a toolbar chip (provider chip shows the provider name). |
| <a id="chiptopage"></a>`chipToPage` — app/src/components/AgentBar.tsx | Map a toolbar chip identifier to its dockview page id. |
| <a id="statustone"></a>`statusTone` — app/src/components/AgentGrid.tsx | Color-code an agent card chip by agent status (running vs. anything else). |
| <a id="agentgrid"></a>`AgentGrid` — app/src/components/AgentGrid.tsx | Grid of agent cards from `/api/agents` with click-to-select into the shared store. |
| <a id="panelfactory"></a>`panelFactory` — app/src/components/CockpitShell.tsx | Map a page id to its React page component for the dockview layout. |
| <a id="pagepanel"></a>`PagePanel` — app/src/components/CockpitShell.tsx | Dockview panel wrapper that renders whichever page component the factory returns. |
| <a id="cockpitshell"></a>`CockpitShell` — app/src/components/CockpitShell.tsx | Main workspace shell: dockview panels, agent bar, hash routing, and page focus logic. |
| <a id="onbarchip"></a>`onBarChip` — app/src/components/CockpitShell.tsx | Handle toolbar chip clicks: reload on RESTART, otherwise focus the mapped page. |
| <a id="ghuichip"></a>`GhuiChip` — app/src/components/GhuiChip.tsx | Small animated pill button used across the UI for chips and status labels. |
| <a id="terminalpane"></a>`TerminalPane` — app/src/components/TerminalPane.tsx | Embedded xterm.js terminal bridged over WebSocket to the server's node-pty backend. |
| <a id="launchshellout"></a>`launchShellOut` — app/src/components/TerminalPane.tsx | Ask the API to shell out to a registered external emulator (Foot/Ghostty registry). |
| <a id="fetchjson"></a>`fetchJson` — app/src/lib/api.ts | Typed fetch helper that throws on non-OK responses and parses JSON. |
| <a id="ptywebsocketurl"></a>`ptyWebSocketUrl` — app/src/lib/api.ts | Build the ws/wss URL for `/ws/pty`, adapting to Tauri dev vs. served host. |
| <a id="modelsview"></a>`ModelsView` — app/src/pages/ModelsView.tsx | Static catalog of available models shown inside the COMPUTERS page (toggle with `m`). |
| <a id="splashpage"></a>`SplashPage` — app/src/pages/SplashPage.tsx | Landing screen with GitHub OAuth device-flow login and health status. |
| <a id="memorypage"></a>`MemoryPage` — app/src/pages/data-pages.tsx | MEMORY page listing memory entries from `/api/memory` with the fail-closed badge. |
| <a id="computerspage"></a>`ComputersPage` — app/src/pages/data-pages.tsx | COMPUTERS page listing reachable nodes; `m` toggles the embedded models catalog. |
| <a id="onkey"></a>`onKey` — app/src/pages/data-pages.tsx | Keyboard shortcut handler toggling the models view inside COMPUTERS. |
| <a id="agentpage"></a>`AgentPage` — app/src/pages/static-pages.tsx | AGENT page composing the agent grid above the terminal pane. |
| <a id="filespage"></a>`FilesPage` — app/src/pages/static-pages.tsx | FILES page describing the Neovim-follower file watch surfaced by `cockpit files`. |
| <a id="diffpage"></a>`DiffPage` — app/src/pages/static-pages.tsx | DIFF page describing the scoped git diff bands (staged/working/untracked). |
| <a id="mappage"></a>`MapPage` — app/src/pages/static-pages.tsx | MAP page describing mermaid rendering via the showmegraphs adapter. |
| <a id="setuppage"></a>`SetupPage` — app/src/pages/static-pages.tsx | SETUP page describing auth/provider/model/git/plugin/audit configuration. |
| <a id="prspage"></a>`PrsPage` — app/src/pages/static-pages.tsx | PRS page describing the GitHub PR dashboard backed by the gh CLI. |
| <a id="benchpage"></a>`BenchPage` — app/src/pages/static-pages.tsx | BENCH page documenting the read-only Proctor harness and its pnpm entry points. |
| <a id="check"></a>`check` — bench/cockpit/capability-matrix.sh | Record one capability-matrix result row and tally pass/warn/fail counts. |
| <a id="cleanup"></a>`cleanup` — bench/cockpit/capture-screenshots.sh | Kill the screenshot-run API and UI servers on script exit. |
| <a id="start_api"></a>`start_api` — bench/cockpit/capture-screenshots.sh | Start the cockpit web API on a health check loop if it is not already up. |
| <a id="start_ui"></a>`start_ui` — bench/cockpit/capture-screenshots.sh | Start the Vite dev UI if it is not already serving, waiting for readiness. |
| <a id="shot"></a>`shot` — bench/cockpit/capture-screenshots.sh | Capture one Playwright screenshot of a cockpit URL into the output dir. |
| <a id="need"></a>`need` — bench/cockpit/doctor.sh | Assert one required binary exists for the bench environment, tallying results. |
| <a id="check-2"></a>`check` — bench/cockpit/hostinger-h0-verify.sh | Record one hostinger verification result row with pass/fail tally. |
| <a id="check-3"></a>`check` — bench/cockpit/input-matrix.sh | Record one input-matrix (keyboard/touch) result row with pass/fail tally. |
| <a id="check-4"></a>`check` — bench/cockpit/surface-matrix.sh | Record one surface-matrix result row with pass/fail tally. |
| <a id="prepare_session"></a>`prepare_session` — bin/cockpit | Migrate a legacy session name and reload views when entering the cockpit session. |
| <a id="pane_for"></a>`pane_for` — bin/cockpit-adapt | Find the tmux pane id carrying a given cockpit role in the session. |
| <a id="window_for"></a>`window_for` — bin/cockpit-adapt | Resolve the tmux window id that contains a given pane. |
| <a id="tmux_fg"></a>`tmux_fg` — bin/cockpit-adapt | Emit a tmux `#[fg=...]` color spec from the active theme with fallback. |
| <a id="refresh_display_labels"></a>`refresh_display_labels` — bin/cockpit-adapt | Cheaply refresh tmux status labels on resize/CPR without re-laying out panes. |
| <a id="update_git_chrome"></a>`update_git_chrome` — bin/cockpit-adapt | Update the tmux status chrome with worktree, branch, and dirty state. |
| <a id="apply_hush"></a>`apply_hush` — bin/cockpit-adapt | Silence tmux bells, activity monitoring, and titles for a calm session. |
| <a id="apply_keyboard_chrome"></a>`apply_keyboard_chrome` — bin/cockpit-adapt | Apply the desktop/keyboard tmux chrome: mouse, prefix, top status bar. |
| <a id="apply_touch_chrome"></a>`apply_touch_chrome` — bin/cockpit-adapt | Apply the touch/Termius tmux chrome with a longer escape-time for SGR mouse. |
| <a id="layout_tabs"></a>`layout_tabs` — bin/cockpit-adapt | Break FILES/SETUP/DIFF/MAP panes out into their own named tab windows. |
| <a id="layout_pad"></a>`layout_pad` — bin/cockpit-adapt | Arrange the pad-style layout: runtime plus auxiliary panes in place. |
| <a id="hide_auxiliary_tabs"></a>`hide_auxiliary_tabs` — bin/cockpit-adapt | Hide auxiliary windows from the tab row when they are not on the runtime window. |
| <a id="normalize_primary_tabs"></a>`normalize_primary_tabs` — bin/cockpit-adapt | Enforce the canonical tab order/numbering for the primary cockpit windows. |
| <a id="usage"></a>`usage` — bin/cockpit-agent-config | Print the `cockpit profile` subcommand help text. |
| <a id="print_report"></a>`print_report` — bin/cockpit-agent-config | Print the agent profile inspection report (markers, skills, missing skills). |
| <a id="print_profile"></a>`print_profile` — bin/cockpit-agent-config | Print the raw agent profile file fields (paths, enabled flag). |
| <a id="cockpit_agent_trim"></a>`cockpit_agent_trim` — bin/cockpit-agent-lib | Strip leading/trailing whitespace from a value. |
| <a id="cockpit_agent_profile_field"></a>`cockpit_agent_profile_field` — bin/cockpit-agent-lib | Read one key from the INI-style agent profile file, skipping comments and sections. |
| <a id="cockpit_agent_expand_path"></a>`cockpit_agent_expand_path` — bin/cockpit-agent-lib | Expand a leading `~` in a configured path to `$HOME`. |
| <a id="cockpit_agent_profile_skills_dir"></a>`cockpit_agent_profile_skills_dir` — bin/cockpit-agent-lib | Resolve the skills directory from the profile, defaulting under the config dir. |
| <a id="cockpit_agent_profile_enabled"></a>`cockpit_agent_profile_enabled` — bin/cockpit-agent-lib | Report whether the cockpit agent profile is enabled (truthy `enabled=` value). |
| <a id="cockpit_agent_skill_id"></a>`cockpit_agent_skill_id` — bin/cockpit-agent-lib | Derive a skill id from a skill file path by stripping directories and extensions. |
| <a id="cockpit_agent_skill_allowed"></a>`cockpit_agent_skill_allowed` — bin/cockpit-agent-lib | Check a skill id against the profile's allow-list (`skills=` field). |
| <a id="cockpit_agent_skill_files"></a>`cockpit_agent_skill_files` — bin/cockpit-agent-lib | List allowed `*.md`/`*.mdc` skill files in the skills directory, sorted. |
| <a id="cockpit_agent_skill_count"></a>`cockpit_agent_skill_count` — bin/cockpit-agent-lib | Count the allowed skill files available to the agent profile. |
| <a id="cockpit_agent_provider_family"></a>`cockpit_agent_provider_family` — bin/cockpit-agent-lib | Normalize a provider id to its family (codex, grok, anthropic, cursor, generic). |
| <a id="cockpit_agent_default_paths"></a>`cockpit_agent_default_paths` — bin/cockpit-agent-lib | Emit the default AGENTS.md-style marker paths searched for a provider family. |
| <a id="cockpit_agent_configured_paths"></a>`cockpit_agent_configured_paths` — bin/cockpit-agent-lib | Emit explicitly configured agent file paths from the provider registry. |
| <a id="cockpit_agent_paths_are_configured"></a>`cockpit_agent_paths_are_configured` — bin/cockpit-agent-lib | Test whether any agent file paths are explicitly configured for a provider. |
| <a id="cockpit_agent_root"></a>`cockpit_agent_root` — bin/cockpit-agent-lib | Canonicalize a project root directory or fail when it is not a directory. |
| <a id="cockpit_agent_home_scope"></a>`cockpit_agent_home_scope` — bin/cockpit-agent-lib | Test whether a root resolves to the user's `$HOME` (home-scope guard). |
| <a id="cockpit_agent_safe_spec"></a>`cockpit_agent_safe_spec` — bin/cockpit-agent-lib | Reject unsafe relative path specs (absolute paths, `..` traversal). |
| <a id="cockpit_agent_relpath"></a>`cockpit_agent_relpath` — bin/cockpit-agent-lib | Compute a project-relative path, resolving symlinks against the root. |
| <a id="cockpit_agent_candidate_for_spec"></a>`cockpit_agent_candidate_for_spec` — bin/cockpit-agent-lib | Resolve a safe spec to a candidate target path under the project root. |
| <a id="cockpit_agent_is_home_hidden_target"></a>`cockpit_agent_is_home_hidden_target` — bin/cockpit-agent-lib | Detect hidden agent dirs (`.codex`, `.agent`, …) when the root is `$HOME`. |
| <a id="cockpit_agent_marker_list"></a>`cockpit_agent_marker_list` — bin/cockpit-agent-lib | List agent marker files/dirs present under a root, skipping home-hidden targets. |
| <a id="cockpit_agent_file_style"></a>`cockpit_agent_file_style` — bin/cockpit-agent-lib | Classify a target file's comment style (hash, html, unsupported) for block injection. |
| <a id="cockpit_agent_managed_present"></a>`cockpit_agent_managed_present` — bin/cockpit-agent-lib | Test whether a file already contains the cockpit managed-profile block. |
| <a id="cockpit_agent_skill_present"></a>`cockpit_agent_skill_present` — bin/cockpit-agent-lib | Test whether a specific skill marker is already present in a file. |
| <a id="cockpit_agent_inspect"></a>`cockpit_agent_inspect` — bin/cockpit-agent-lib | Inspect a project/provider pair and export its target state into env vars. |
| <a id="cockpit_agent_target_state_label"></a>`cockpit_agent_target_state_label` — bin/cockpit-agent-lib | Render the inspected target state (valid, missing, home-scope, …) as a label. |
| <a id="cockpit_agent_render_block"></a>`cockpit_agent_render_block` — bin/cockpit-agent-lib | Render the managed profile block (comment style aware) for a target file. |
| <a id="cockpit_agent_remove_managed_block"></a>`cockpit_agent_remove_managed_block` — bin/cockpit-agent-lib | Strip a previously injected managed block from a file via awk. |
| <a id="cockpit_agent_apply_profile"></a>`cockpit_agent_apply_profile` — bin/cockpit-agent-lib | Apply the agent profile to a target file with backup, dry-run, and status reporting. |
| <a id="pass_check"></a>`pass_check` — bin/cockpit-audit | Record a passing audit check with green OK output. |
| <a id="warn_check"></a>`warn_check` — bin/cockpit-audit | Record a warning audit check with yellow WARN output. |
| <a id="fail_check"></a>`fail_check` — bin/cockpit-audit | Record a failing audit check with red FAIL output. |
| <a id="has_command"></a>`has_command` — bin/cockpit-audit | Test whether a command exists on PATH. |
| <a id="plugin_catalog_health"></a>`plugin_catalog_health` — bin/cockpit-audit | Audit the Codex plugin catalog availability via the codex CLI. |
| <a id="cockpit_plugin_health"></a>`cockpit_plugin_health` — bin/cockpit-audit | Audit the cockpit-native plugin registry for the CPR plugin. |
| <a id="agent_target_health"></a>`agent_target_health` — bin/cockpit-audit | Audit the live runtime pane's agent target (root, provider, state). |
| <a id="open_popup"></a>`open_popup` — bin/cockpit-auth | Open the provider login in a tmux popup, or re-exec outside tmux. |
| <a id="run_login"></a>`run_login` — bin/cockpit-auth | Run a provider's configured login command inside the auth popup. |
| <a id="draw_gate"></a>`draw_gate` — bin/cockpit-auth | Draw the auth gate screen offering Auth vs. skip choices. |
| <a id="gate"></a>`gate` — bin/cockpit-auth | Interactive gate loop: sign-in check, draw, and handle the user's auth choice. |
| <a id="pick"></a>`pick` — bin/cockpit-auth | Numbered picker over configured providers for the OAuth login flow. |
| <a id="cockpit_auth_ensure_config"></a>`cockpit_auth_ensure_config` — bin/cockpit-auth-lib | Ensure the auth config dir/file exist, seeding from legacy or staged providers. |
| <a id="cockpit_auth_files"></a>`cockpit_auth_files` — bin/cockpit-auth-lib | List the main providers.conf plus all per-provider `*.conf` files. |
| <a id="cockpit_auth_kind"></a>`cockpit_auth_kind` — bin/cockpit-auth-lib | Return a provider's `kind=` field (cli-oauth, runtime, …). |
| <a id="cockpit_auth_ids"></a>`cockpit_auth_ids` — bin/cockpit-auth-lib | List provider ids across auth files, optionally filtered by kind. |
| <a id="cockpit_auth_field"></a>`cockpit_auth_field` — bin/cockpit-auth-lib | Read one `key=` field from a provider's INI-style section. |
| <a id="cockpit_auth_run_probe"></a>`cockpit_auth_run_probe` — bin/cockpit-auth-lib | Run a provider probe command under a timeout, silencing output. |
| <a id="cockpit_auth_grok_cache_ready"></a>`cockpit_auth_grok_cache_ready` — bin/cockpit-auth-lib | Check the Grok auth.json cache for a non-expired cached token. |
| <a id="cockpit_auth_probe_configured"></a>`cockpit_auth_probe_configured` — bin/cockpit-auth-lib | Test whether a provider has any probe/auth_check configured (or a builtin). |
| <a id="cockpit_auth_probe"></a>`cockpit_auth_probe` — bin/cockpit-auth-lib | Run the provider-specific auth probe (with codex/grok builtins as fallback). |
| <a id="cockpit_auth_check"></a>`cockpit_auth_check` — bin/cockpit-auth-lib | Run a provider's configured `check` command to verify the CLI exists. |
| <a id="cockpit_auth_signed_in"></a>`cockpit_auth_signed_in` — bin/cockpit-auth-lib | Report signed-in state: CLI check passes and the provider probe succeeds. |
| <a id="cockpit_auth_state"></a>`cockpit_auth_state` — bin/cockpit-auth-lib | Classify provider auth state (cli-missing, ready, …) for display. |
| <a id="cockpit_popup_use_window"></a>`cockpit_popup_use_window` — bin/cockpit-auth-lib | Decide whether auth popups should use a full window (touch/Termius) vs. popup. |
| <a id="cockpit_popup_slug"></a>`cockpit_popup_slug` — bin/cockpit-auth-lib | Make a short uppercase slug for naming popup windows. |
| <a id="cockpit_popup_any"></a>`cockpit_popup_any` — bin/cockpit-auth-lib | Test whether any cockpit popup window is currently open. |
| <a id="cockpit_popup_home"></a>`cockpit_popup_home` — bin/cockpit-auth-lib | Return focus to the runtime pane before/after popup work. |
| <a id="cockpit_popup_dismiss"></a>`cockpit_popup_dismiss` — bin/cockpit-auth-lib | Kill all open cockpit popup windows. |
| <a id="cockpit_popup_gc"></a>`cockpit_popup_gc` — bin/cockpit-auth-lib | Garbage-collect stale popup windows left behind by dead sessions. |
| <a id="cockpit_popup"></a>`cockpit_popup` — bin/cockpit-auth-lib | Show a command in a tmux popup/window, falling back to direct exec outside tmux. |
| <a id="cockpit_auth_ensure"></a>`cockpit_auth_ensure` — bin/cockpit-auth-lib | Ensure a provider is signed in, running its login command when needed. |
| <a id="cockpit_auth_login_cmd"></a>`cockpit_auth_login_cmd` — bin/cockpit-auth-lib | Resolve the login command for a provider (configured or builtin). |
| <a id="cockpit_auth_ready_cmd"></a>`cockpit_auth_ready_cmd` — bin/cockpit-auth-lib | Return a provider's configured `on_ready` hook command. |
| <a id="cockpit_auth_label"></a>`cockpit_auth_label` — bin/cockpit-auth-lib | Return a provider's display label, falling back to its id. |
| <a id="rid"></a>`rid` — bin/cockpit-bar | Read the active runtime provider id from the tmux session option. |
| <a id="label-2"></a>`label` — bin/cockpit-bar | Resolve a provider id to its display label for the TUI bar. |
| <a id="pane_size"></a>`pane_size` — bin/cockpit-bar | Measure the bar pane's columns/rows, defaulting to 80x6. |
| <a id="chip_gap"></a>`chip_gap` — bin/cockpit-bar | Compute the gap between bar chips, collapsing to zero on narrow/touch screens. |
| <a id="center"></a>`center` — bin/cockpit-bar | Center (or truncate) text within a fixed width for bar rendering. |
| <a id="chip_row"></a>`chip_row` — bin/cockpit-bar | Render one row of a clickable chip box with style and visibility flag. |
| <a id="draw"></a>`draw` — bin/cockpit-bar | Draw the full AGENT toolbar: provider, PRS, model, and restart chips. |
| <a id="open_prs"></a>`open_prs` — bin/cockpit-bar | Jump to the existing PRS pane, selecting its window directly. |
| <a id="open_runtime"></a>`open_runtime` — bin/cockpit-bar | Open provider switching in the persistent SETUP terminal via cockpit-touch. |
| <a id="open_model"></a>`open_model` — bin/cockpit-bar | Open the model picker in the persistent SETUP terminal via cockpit-touch. |
| <a id="restart_runtime"></a>`restart_runtime` — bin/cockpit-bar | Restart the agent runtime through `cockpit-restart`. |
| <a id="which_btn"></a>`which_btn` — bin/cockpit-bar | Map an x click coordinate to the chip button under it. |
| <a id="now_ms"></a>`now_ms` — bin/cockpit-bar | Current time in epoch milliseconds for tap debouncing. |
| <a id="tap_debounce"></a>`tap_debounce` — bin/cockpit-bar | Suppress duplicate taps using a stamp file in the runtime dir. |
| <a id="handle_sgr"></a>`handle_sgr` — bin/cockpit-bar | Parse one SGR mouse escape sequence from stdin into button/x/y. |
| <a id="run_btn"></a>`run_btn` — bin/cockpit-bar | Dispatch a resolved chip name to its action (prs, runtime, model, restart). |
| <a id="draw-2"></a>`draw` — bin/cockpit-bench | Render the BENCH screen: harness path, last matrix tail, funnel/serve state. |
| <a id="draw-3"></a>`draw` — bin/cockpit-computers | Render the COMPUTERS screen from the API, with a static fallback table. |
| <a id="need_gh"></a>`need_gh` — bin/cockpit-config | Require the gh CLI and an authenticated session, else exit with guidance. |
| <a id="safe_copy"></a>`safe_copy` — bin/cockpit-config | Copy a file only after refusing when it looks like it contains secrets. |
| <a id="tmux_fg-2"></a>`tmux_fg` — bin/cockpit-diff | Emit a tmux `#[fg=...]` color spec from the active theme with fallback. |
| <a id="update_git_chrome-2"></a>`update_git_chrome` — bin/cockpit-diff | Update the diff pane's git chrome (worktree, branch, dirty state). |
| <a id="refresh_git_chrome"></a>`refresh_git_chrome` — bin/cockpit-diff | Re-read git status porcelain and refresh the chrome without repainting. |
| <a id="clear_screen"></a>`clear_screen` — bin/cockpit-diff | Clear the terminal screen (shared helper). |
| <a id="remember"></a>`remember` — bin/cockpit-diff | Remember a recently changed path, skipping noise paths and duplicates. |
| <a id="render"></a>`render` — bin/cockpit-diff | Render the scoped git diff view: staged, working, and untracked bands. |
| <a id="maybe_render"></a>`maybe_render` — bin/cockpit-diff | Render the diff only when visible; otherwise just refresh the chrome metadata. |
| <a id="window_of"></a>`window_of` — bin/cockpit-ensure-bar | Resolve the tmux window id for a given target. |
| <a id="tag_bar"></a>`tag_bar` — bin/cockpit-ensure-bar | Tag a pane as the BAR role and pin its height. |
| <a id="hide_from_tabs"></a>`hide_from_tabs` — bin/cockpit-ensure-bar | Hide a pane's window from the tab row and disable rename/activity noise. |
| <a id="cleanup-2"></a>`cleanup` — bin/cockpit-files | Tear down the file watcher: kill it, remove the socket and pid file. |
| <a id="open_in_nvim"></a>`open_in_nvim` — bin/cockpit-files | Ask the listening Neovim instance to open a changed path via remote-send. |
| <a id="follow_edits"></a>`follow_edits` — bin/cockpit-files | Watch the project with inotify and follow agent-written files in Neovim. |
| <a id="cleanup-3"></a>`cleanup` — bin/cockpit-gate | Restore the terminal (mouse off, sane stty) when the gate exits. |
| <a id="draw-4"></a>`draw` — bin/cockpit-gate | Draw the modality gate screen (desktop vs. touch choice). |
| <a id="read_byte"></a>`read_byte` — bin/cockpit-gate | Read one byte from the tty with a timeout for the gate input loop. |
| <a id="enable_mouse"></a>`enable_mouse` — bin/cockpit-gate | Enable SGR mouse reporting on the tty for the gate. |
| <a id="parse_idle_event"></a>`parse_idle_event` — bin/cockpit-gate | Parse one gate input event (key or SGR mouse) from the tty. |
| <a id="cockpit_config_home"></a>`cockpit_config_home` — bin/cockpit-lib | Resolve the canonical cockpit config home (env override, auth home, or XDG). |
| <a id="cockpit_legacy_config_home"></a>`cockpit_legacy_config_home` — bin/cockpit-lib | Resolve the legacy codex-cockpit config home for migration reads. |
| <a id="cockpit_config_read_path"></a>`cockpit_config_read_path` — bin/cockpit-lib | Pick the canonical config path, falling back to the legacy one when present. |
| <a id="cockpit_overlay_path"></a>`cockpit_overlay_path` — bin/cockpit-lib | Resolve the tmux overlay config path (canonical, legacy, or env override). |
| <a id="cockpit_session_name"></a>`cockpit_session_name` — bin/cockpit-lib | Resolve the tmux session name from env with `cockpit` default. |
| <a id="cockpit_session_is_managed"></a>`cockpit_session_is_managed` — bin/cockpit-lib | Test whether a tmux session name belongs to cockpit's managed namespace. |
| <a id="cockpit_migrate_legacy_session"></a>`cockpit_migrate_legacy_session` — bin/cockpit-lib | Rename a legacy `codex-cockpit` tmux session to `cockpit` in place. |
| <a id="cockpit_runtime_home"></a>`cockpit_runtime_home` — bin/cockpit-lib | Resolve the XDG runtime dir for cockpit state. |
| <a id="cockpit_legacy_runtime_home"></a>`cockpit_legacy_runtime_home` — bin/cockpit-lib | Resolve the legacy codex-cockpit runtime dir for migration reads. |
| <a id="cockpit_shell_quote"></a>`cockpit_shell_quote` — bin/cockpit-lib | Single-quote a value for safe embedding in shell commands. |
| <a id="cockpit_resolve_codex_bin"></a>`cockpit_resolve_codex_bin` — bin/cockpit-lib | Resolve the codex CLI binary, preferring `CODEX_CLI_BIN` and guarding mise shims. |
| <a id="cockpit_codex_runtime_command"></a>`cockpit_codex_runtime_command` — bin/cockpit-lib | Build the runtime command line with the resolved codex binary quoted in. |
| <a id="cockpit_runtime_color_setup"></a>`cockpit_runtime_color_setup` — bin/cockpit-lib | Export sane truecolor TERM/COLORTERM defaults for provider panes. |
| <a id="cockpit_theme_colors_file"></a>`cockpit_theme_colors_file` — bin/cockpit-lib | Locate the active Omarchy theme colors.toml when readable. |
| <a id="cockpit_theme_hex"></a>`cockpit_theme_hex` — bin/cockpit-lib | Look up a theme color hex by key with caching and fallback. |
| <a id="cockpit_theme_load_cache"></a>`cockpit_theme_load_cache` — bin/cockpit-lib | Preload the theme hex cache from the colors.toml file. |
| <a id="cockpit_theme_color_is_saturated"></a>`cockpit_theme_color_is_saturated` — bin/cockpit-lib | Test whether a hex color is saturated enough for diff backgrounds. |
| <a id="cockpit_theme_contrast_key"></a>`cockpit_theme_contrast_key` — bin/cockpit-lib | Pick a readable foreground key contrasting with a given background hex. |
| <a id="cockpit_theme_load_diff_cache"></a>`cockpit_theme_load_diff_cache` — bin/cockpit-lib | Resolve themed diff add/delete background colors with saturation guard. |
| <a id="cockpit_theme_style"></a>`cockpit_theme_style` — bin/cockpit-lib | Build an ANSI style sequence from theme fg/bg keys and bold flag. |
| <a id="cockpit_theme_reset"></a>`cockpit_theme_reset` — bin/cockpit-lib | Emit the ANSI reset sequence. |
| <a id="cockpit_theme_heading"></a>`cockpit_theme_heading` — bin/cockpit-lib | Emit the themed bold heading style. |
| <a id="cockpit_theme_muted"></a>`cockpit_theme_muted` — bin/cockpit-lib | Emit the themed muted style. |
| <a id="cockpit_theme_add"></a>`cockpit_theme_add` — bin/cockpit-lib | Emit the themed diff-addition foreground style. |
| <a id="cockpit_theme_delete"></a>`cockpit_theme_delete` — bin/cockpit-lib | Emit the themed diff-deletion foreground style. |
| <a id="cockpit_theme_change"></a>`cockpit_theme_change` — bin/cockpit-lib | Emit the themed diff-change (hunk) style. |
| <a id="cockpit_diff_colorize"></a>`cockpit_diff_colorize` — bin/cockpit-lib | Colorize unified diff lines (add/delete/hunk/heading) from stdin. |
| <a id="cockpit_diff_status_colorize"></a>`cockpit_diff_status_colorize` — bin/cockpit-lib | Colorize `git status --short` lines from stdin. |
| <a id="cockpit_is_text_ext"></a>`cockpit_is_text_ext` — bin/cockpit-lib | Test whether a path looks like a text file by extension or known name. |
| <a id="cockpit_is_noise_path"></a>`cockpit_is_noise_path` — bin/cockpit-lib | Test whether a path is watcher noise (build dirs, temp/swap files). |
| <a id="cockpit_view_visible"></a>`cockpit_view_visible` — bin/cockpit-lib | Test whether the current pane's view is visible (active, unzoomed-obscured). |
| <a id="cockpit_wake_dir"></a>`cockpit_wake_dir` — bin/cockpit-lib | Resolve the per-session wake/event directory under the runtime home. |
| <a id="cockpit_legacy_wake_dir"></a>`cockpit_legacy_wake_dir` — bin/cockpit-lib | Resolve the legacy per-session wake directory for migration. |
| <a id="cockpit_install_wake"></a>`cockpit_install_wake` — bin/cockpit-lib | Install a USR1 wake trap and pid file so tabs can wake this watcher. |
| <a id="cockpit_event_open"></a>`cockpit_event_open` — bin/cockpit-lib | Open the event FIFO for a watcher: no idle polling, block on inotify/USR1. |
| <a id="cockpit_event_watch"></a>`cockpit_event_watch` — bin/cockpit-lib | Start an inotifywait feeding paths into the event FIFO. |
| <a id="cockpit_event_read"></a>`cockpit_event_read` — bin/cockpit-lib | Block-read one event from the event FIFO into `$COCKPIT_EVENT`. |
| <a id="cockpit_event_drain"></a>`cockpit_event_drain` — bin/cockpit-lib | Drain pending FIFO events, handling WAKE signals, printing the rest. |
| <a id="cockpit_event_close"></a>`cockpit_event_close` — bin/cockpit-lib | Kill inotify helpers, close the FIFO fd, and remove the FIFO. |
| <a id="cockpit_hush_session"></a>`cockpit_hush_session` — bin/cockpit-lib | Silence bells/activity/titles across a whole tmux session. |
| <a id="cockpit_coalesce"></a>`cockpit_coalesce` — bin/cockpit-lib | Coalesce a burst of stdin events within the debounce window. |
| <a id="cockpit_state_file"></a>`cockpit_state_file` — bin/cockpit-lib | Path of the persisted cockpit state file (session/modality/endpoint). |
| <a id="cockpit_state_write"></a>`cockpit_state_write` — bin/cockpit-lib | Atomically write session/modality/endpoint state via temp file + rename. |
| <a id="cockpit_fingerprint_files"></a>`cockpit_fingerprint_files` — bin/cockpit-lib | Fingerprint files by mtime+size so watchers can skip unchanged redraws. |
| <a id="attach_client"></a>`attach_client` — bin/cockpit-main | Attach to the cockpit tmux session via cockpit-client, falling back to plain tmux. |
| <a id="is_project_dir"></a>`is_project_dir` — bin/cockpit-main | Heuristic: does a directory look like a project (git, README, manifests)? |
| <a id="usable_git_root"></a>`usable_git_root` — bin/cockpit-main | Reject an empty `$HOME` git worktree as a cockpit root; accept real roots. |
| <a id="reap_ghost_sessions"></a>`reap_ghost_sessions` — bin/cockpit-main | Kill detached leftover cockpit-managed tmux sessions. |
| <a id="quote_sh"></a>`quote_sh` — bin/cockpit-main | Single-quote a value for the session-creation command line. |
| <a id="tag"></a>`tag` — bin/cockpit-main | Tag a new pane with its title and cockpit role. |
| <a id="draw-5"></a>`draw` — bin/cockpit-memory | Render the MEMORY screen from the API with a fail-closed offline notice. |
| <a id="find_diagram"></a>`find_diagram` — bin/cockpit-mermaid-watch | Locate the mermaid diagram file (explicit path or `docs/architecture.mmd`). |
| <a id="write_canon_diagram"></a>`write_canon_diagram` — bin/cockpit-mermaid-watch | Write the canonical default mermaid flowchart when none exists. |
| <a id="ensure_diagram"></a>`ensure_diagram` — bin/cockpit-mermaid-watch | Ensure a diagram exists, creating the canonical one as a fallback. |
| <a id="clear_screen-2"></a>`clear_screen` — bin/cockpit-mermaid-watch | Clear the terminal screen (shared helper). |
| <a id="header"></a>`header` — bin/cockpit-mermaid-watch | Print the MAP pane header with the active diagram path. |
| <a id="file_fp"></a>`file_fp` — bin/cockpit-mermaid-watch | Fingerprint a diagram file by mtime+size for change detection. |
| <a id="render_graph"></a>`render_graph` — bin/cockpit-mermaid-watch | Render the diagram via the configured renderer (showmegraphs plugin). |
| <a id="render-2"></a>`render` — bin/cockpit-mermaid-watch | Re-render the mermaid graph when its fingerprint changed. |
| <a id="maybe_render-2"></a>`maybe_render` — bin/cockpit-mermaid-watch | Render only when visible; otherwise mark the view dirty for later. |
| <a id="usage-2"></a>`usage` — bin/cockpit-plugin | Print the `cockpit plugin` subcommand help text. |
| <a id="manifest_field"></a>`manifest_field` — bin/cockpit-plugin | Read one field from a plugin's `plugin.conf` manifest. |
| <a id="plugin_dir"></a>`plugin_dir` — bin/cockpit-plugin | Resolve an installed or bundled plugin directory by id (with cpr alias). |
| <a id="validate_plugin"></a>`validate_plugin` — bin/cockpit-plugin | Validate a plugin manifest: id, cockpit type, safe relative entrypoint. |
| <a id="list_plugins"></a>`list_plugins` — bin/cockpit-plugin | List validated installed and bundled cockpit-native plugins. |
| <a id="run_plugin"></a>`run_plugin` — bin/cockpit-plugin | Execute a validated plugin's entrypoint with arguments. |
| <a id="now_ms-2"></a>`now_ms` — bin/cockpit-pointer | Current time in epoch milliseconds for pointer debouncing. |
| <a id="debounce"></a>`debounce` — bin/cockpit-pointer | Drop pointer events arriving within the debounce window of the last one. |
| <a id="session_of"></a>`session_of` — bin/cockpit-pointer | Resolve the target tmux session, preferring an explicitly managed one. |
| <a id="role_of"></a>`role_of` — bin/cockpit-pointer | Read a pane's cockpit role option. |
| <a id="window_of-2"></a>`window_of` — bin/cockpit-pointer | Resolve the tmux window id for a target pane. |
| <a id="mouse_target"></a>`mouse_target` — bin/cockpit-pointer | Evaluate a tmux format string for the mouse event target. |
| <a id="is_pane_id"></a>`is_pane_id` — bin/cockpit-pointer | Test whether a string looks like a tmux pane id (`%N`). |
| <a id="trim"></a>`trim` — bin/cockpit-pointer | Strip leading/trailing whitespace from a value. |
| <a id="bar_click"></a>`bar_click` — bin/cockpit-pointer | Dispatch a bar tap: resolve the chip, debounce, and exec the click handler. |
| <a id="cleanup-4"></a>`cleanup` — bin/cockpit-popup | Restore focus to the runtime pane when a popup closes. |
| <a id="emit"></a>`emit` — bin/cockpit-probe-install | Emit one tmux key-table probe binding that reports keys to cockpit-modality. |
| <a id="usage-3"></a>`usage` — bin/cockpit-profile | Print the `cockpit profile-detect` usage and exit. |
| <a id="in_tailscale_cgnat"></a>`in_tailscale_cgnat` — bin/cockpit-profile | Test whether an IP is in the Tailscale CGNAT range (100.64/10). |
| <a id="pid_comm"></a>`pid_comm` — bin/cockpit-profile | Read a process's command name from `/proc`. |
| <a id="pid_env"></a>`pid_env` — bin/cockpit-profile | Dump a process's environment from `/proc` as newline-separated pairs. |
| <a id="pid_ppid"></a>`pid_ppid` — bin/cockpit-profile | Read a process's parent pid from `/proc`. |
| <a id="env_val"></a>`env_val` — bin/cockpit-profile | Extract one variable's value from a dumped environment blob. |
| <a id="fingerprint_pid"></a>`fingerprint_pid` — bin/cockpit-profile | Walk a pid's ancestry collecting comms and env into a fingerprint blob. |
| <a id="classify_fp"></a>`classify_fp` — bin/cockpit-profile | Classify a fingerprint blob into a client profile (foot, termius, …). |
| <a id="classify_pid"></a>`classify_pid` — bin/cockpit-profile | Fingerprint and classify a single pid's client profile. |
| <a id="session_profile"></a>`session_profile` — bin/cockpit-profile | Profile all clients of a tmux session into an overall modality verdict. |
| <a id="shell_quote"></a>`shell_quote` — bin/cockpit-reload-views | Single-quote a value for safe shell embedding. |
| <a id="pane_for-2"></a>`pane_for` — bin/cockpit-reload-views | Find a role's pane, with legacy fallbacks for the runtime role. |
| <a id="setup_has_editor"></a>`setup_has_editor` — bin/cockpit-reload-views | Test whether a setup pane is running an editor (nvim child). |
| <a id="normalize_setup_edit_windows"></a>`normalize_setup_edit_windows` — bin/cockpit-reload-views | Deduplicate legacy SETUP-EDIT windows, keeping the newest editor. |
| <a id="reload_bar"></a>`reload_bar` — bin/cockpit-reload-views | Respawn the BAR pane in place and re-tag it. |
| <a id="ensure_setup"></a>`ensure_setup` — bin/cockpit-reload-views | Ensure the SETUP pane exists, hiding legacy setup windows. |
| <a id="reload_view"></a>`reload_view` — bin/cockpit-reload-views | Reload one view pane (role) in place from the project root. |
| <a id="runtime_pane"></a>`runtime_pane` — bin/cockpit-runtime | Find the pane holding the active runtime role. |
| <a id="pane_for_id"></a>`pane_for_id` — bin/cockpit-runtime | Find the pane running a specific runtime id. |
| <a id="pane_pid"></a>`pane_pid` — bin/cockpit-runtime | Read the OS pid of a tmux pane's process. |
| <a id="pause_pane"></a>`pause_pane` — bin/cockpit-runtime | SIGSTOP a pane's process group to pause an agent cheaply. |
| <a id="resume_pane"></a>`resume_pane` — bin/cockpit-runtime | SIGCONT a paused pane's process group. |
| <a id="hide_rt_window"></a>`hide_rt_window` — bin/cockpit-runtime | Hide a runtime window from tabs and hush its notifications. |
| <a id="show_rt_window"></a>`show_rt_window` — bin/cockpit-runtime | Restore a hidden runtime window's tab visibility. |
| <a id="root_path"></a>`root_path` — bin/cockpit-runtime | Report the runtime pane's working directory (project root). |
| <a id="hush_window"></a>`hush_window` — bin/cockpit-runtime | Silence bell/activity monitoring on one window. |
| <a id="ensure_runtime_auth"></a>`ensure_runtime_auth` — bin/cockpit-runtime | Ensure the provider is signed in before spawning its runtime. |
| <a id="spawn_runtime"></a>`spawn_runtime` — bin/cockpit-runtime | Spawn a provider's runtime command in a new pane after auth. |
| <a id="save_runtime"></a>`save_runtime` — bin/cockpit-runtime | Persist the active runtime id to the session option and state file. |
| <a id="refresh_agent_target"></a>`refresh_agent_target` — bin/cockpit-runtime | Nudge `cockpit-agent-hook` to refresh the agent target after a switch. |
| <a id="parallel_to"></a>`parallel_to` — bin/cockpit-runtime | Open a second provider as a parallel tab without stealing the agent slot. |
| <a id="switch_to"></a>`switch_to` — bin/cockpit-runtime | Switch the live agent slot to another provider (pausing the current one). |
| <a id="send_model"></a>`send_model` — bin/cockpit-runtime | Send the provider's model-switch keystrokes to the live agent pane. |
| <a id="ask_mode"></a>`ask_mode` — bin/cockpit-runtime | Prompt switch-active vs. parallel-tab vs. cancel for a provider change. |
| <a id="open_omarchy_agent_switcher"></a>`open_omarchy_agent_switcher` — bin/cockpit-runtime | Offer Omarchy's first-party agent picker without disturbing the live agent. |
| <a id="pick-2"></a>`pick` — bin/cockpit-runtime | Numbered picker over kind=runtime providers for switching/spawning. |
| <a id="refresh_agent_state"></a>`refresh_agent_state` — bin/cockpit-setup | Re-run agent inspection for the setup screen's current root/provider. |
| <a id="shell_quote-2"></a>`shell_quote` — bin/cockpit-setup | Single-quote a value for safe shell embedding. |
| <a id="agent_pane"></a>`agent_pane` — bin/cockpit-setup | Find the live agent (runtime role) pane in the session. |
| <a id="project_root"></a>`project_root` — bin/cockpit-setup | Resolve the project root from the agent pane's cwd, else `$PWD`. |
| <a id="active_provider"></a>`active_provider` — bin/cockpit-setup | Resolve the active provider id (session option, pane option, or codex). |
| <a id="provider_cli_ready"></a>`provider_cli_ready` — bin/cockpit-setup | Test whether a provider's CLI check passes. |
| <a id="provider_auth_command"></a>`provider_auth_command` — bin/cockpit-setup | Return a provider's configured auth check command. |
| <a id="provider_auth_configured"></a>`provider_auth_configured` — bin/cockpit-setup | Test whether a provider has auth probing configured. |
| <a id="provider_oauth_ready"></a>`provider_oauth_ready` — bin/cockpit-setup | Test whether a provider's OAuth session is signed in. |
| <a id="provider_status"></a>`provider_status` — bin/cockpit-setup | Compute a cached human-readable status line for a provider. |
| <a id="invalidate_status_cache"></a>`invalidate_status_cache` — bin/cockpit-setup | Clear the provider status cache so the next draw re-probes. |
| <a id="provider_label"></a>`provider_label` — bin/cockpit-setup | Resolve a provider's display label with id fallback. |
| <a id="plugin_json"></a>`plugin_json` — bin/cockpit-setup | Fetch the codex plugin catalog JSON with a timeout guard. |
| <a id="plugin_summary"></a>`plugin_summary` — bin/cockpit-setup | Summarize installed/available codex plugin counts from the catalog JSON. |
| <a id="build_plugin_catalog"></a>`build_plugin_catalog` — bin/cockpit-setup | Build the selectable plugin catalog arrays for the setup UI. |
| <a id="current_target"></a>`current_target` — bin/cockpit-setup | Resolve the current git target dir (saved option or project root). |
| <a id="git_summary"></a>`git_summary` — bin/cockpit-setup | Summarize git state (root, branch) for the setup screen. |
| <a id="draw-6"></a>`draw` — bin/cockpit-setup | Draw the SETUP screen: providers, target, plugins, and action menu. |
| <a id="pause_flow"></a>`pause_flow` — bin/cockpit-setup | Wait for a keypress before returning to the SETUP screen. |
| <a id="choose_provider"></a>`choose_provider` — bin/cockpit-setup | Numbered provider picker, optionally filtered by kind. |
| <a id="native_login_command"></a>`native_login_command` — bin/cockpit-setup | Resolve the native login command for a provider (configured or builtin). |
| <a id="auth_flow"></a>`auth_flow` — bin/cockpit-setup | Interactive auth flow: pick a provider and run its login. |
| <a id="provider_flow"></a>`provider_flow` — bin/cockpit-setup | Explain and start the native provider picker in the SETUP terminal. |
| <a id="trim_value"></a>`trim_value` — bin/cockpit-setup | Strip leading/trailing whitespace from a value. |
| <a id="provider_models"></a>`provider_models` — bin/cockpit-setup | List a provider's models from config or its discovery command. |
| <a id="build_model_catalog"></a>`build_model_catalog` — bin/cockpit-setup | Build the cross-provider model catalog arrays for the picker. |
| <a id="open_native_model_picker"></a>`open_native_model_picker` — bin/cockpit-setup | Send the provider's native model-picker keystrokes to the agent pane. |
| <a id="switch_model_provider"></a>`switch_model_provider` — bin/cockpit-setup | Switch the live agent to another provider before applying a model choice. |
| <a id="select_catalog_model"></a>`select_catalog_model` — bin/cockpit-setup | Apply a catalog model selection and record it in the session option. |
| <a id="model_flow"></a>`model_flow` — bin/cockpit-setup | Interactive model picker across all configured providers. |
| <a id="plugin_flow"></a>`plugin_flow` — bin/cockpit-setup | Interactive codex plugin browser/installer flow. |
| <a id="audit_flow"></a>`audit_flow` — bin/cockpit-setup | Run `cockpit-audit` and report its pass/warn/fail outcome. |
| <a id="agent_target_flow"></a>`agent_target_flow` — bin/cockpit-setup | Show agent target validation details for the current root/provider. |
| <a id="profile_skill_flow"></a>`profile_skill_flow` — bin/cockpit-setup | Interactive flow to apply the cockpit profile and its skills to a target. |
| <a id="choose_target"></a>`choose_target` — bin/cockpit-setup | Pick a project target directory (fzf over `$HOME/Work` when available). |
| <a id="init_target"></a>`init_target` — bin/cockpit-setup | Initialize git in the chosen target, refusing `$HOME`. |
| <a id="edit_config"></a>`edit_config` — bin/cockpit-setup | Open the provider registry for editing in a preserved editor pane. |
| <a id="edit_cockpit_config"></a>`edit_cockpit_config` — bin/cockpit-setup | Menu to edit cockpit.conf, the user profile, or the provider registry. |
| <a id="shell_command"></a>`shell_command` — bin/cockpit-setup | Run an arbitrary shell command and stash its exit status as a message. |
| <a id="run_action"></a>`run_action` — bin/cockpit-setup | Dispatch a SETUP menu keypress to its flow (auth, provider, model, …). |
| <a id="lazy_window"></a>`lazy_window` — bin/cockpit-touch | Select or lazily create a touch-friendly window for a role's command. |
| <a id="add_path"></a>`add_path` — bin/cockpit-vimdiff | Collect a unique changed path for the vimdiff session. |
| <a id="seed_legacy_file"></a>`seed_legacy_file` — install.sh | Seed a config file from the legacy location when the canonical one is missing. |
| <a id="normalize_canonical_namespace"></a>`normalize_canonical_namespace` — install.sh | Rewrite old `~/.config/codex-cockpit` references to the canonical path in place. |
| <a id="normalize_canonical_commands"></a>`normalize_canonical_commands` — install.sh | Rename `codex-cockpit-*` commands to `cockpit-*` in migrated config files. |
| <a id="log"></a>`log` — scripts/generate-arch-docs.sh | Log a namespaced message to stderr. |
| <a id="err"></a>`err` — scripts/generate-arch-docs.sh | Log a namespaced error message to stderr. |
| <a id="compute_tags"></a>`compute_tags` — scripts/generate-arch-docs.sh | Derive the canonical tag list for a function (cli, test, provider, ...). |
| <a id="json_escape"></a>`json_escape` — scripts/generate-arch-docs.sh | JSON-escape a string for safe embedding in generated JSON. |
| <a id="json_str_array"></a>`json_str_array` — scripts/generate-arch-docs.sh | Render a Bash array as a JSON string array. |
| <a id="component_of"></a>`component_of` — scripts/generate-arch-docs.sh | Map a source path to its architecture component (TUI, provider, ...). |
| <a id="md_escape_sig"></a>`md_escape_sig` — scripts/generate-arch-docs.sh | Escape pipe characters in a signature for Markdown table rendering. |
| <a id="need_root"></a>`need_root` — scripts/install-hostinger.sh | Require root for the Hostinger systemd/nginx install steps. |
| <a id="cleanup-5"></a>`cleanup` — tests/test-agent-profile.sh | Remove the test root on exit (test fixture teardown). |
| <a id="cleanup-6"></a>`cleanup` — tests/test-auth-setup.sh | Remove the test root on exit (test fixture teardown). |
| <a id="cleanup-7"></a>`cleanup` — tests/test-cpr-plugin.sh | Remove the test root on exit (test fixture teardown). |
| <a id="cleanup-8"></a>`cleanup` — tests/test-setup-agent-flow.sh | Remove the test root on exit (test fixture teardown). |
| <a id="cleanup-9"></a>`cleanup` — tests/test-setup-flows.sh | Remove the test root on exit (test fixture teardown). |
| <a id="tmux_test"></a>`tmux_test` — tests/test-termius-touch.sh | Run tmux detached from any client env for hermetic touch tests. |
| <a id="cleanup-10"></a>`cleanup` — tests/test-termius-touch.sh | Kill the test tmux server and bar process, then remove the test root. |
