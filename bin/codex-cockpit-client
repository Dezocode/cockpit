#!/usr/bin/env python3
# Legacy compatibility entrypoint; canonical implementation is cockpit-client.
import os
import sys
from pathlib import Path
target = Path(__file__).with_name("cockpit-client")
os.execv(str(target), [str(target), *sys.argv[1:]])
