#!/usr/bin/env bash
set -euo pipefail

# PRs are just the GitHub provider's ready command. Auth is generic and
# configured in ~/.config/cockpit/providers.conf — not hardcoded here.
exec cockpit-auth gate github
